name: dev_to_dev
description: DevToDev package for app analytics.
repository: https://github.com/Ddarkbooked/dev_to_dev
version: 0.0.1


environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  plugin:
    platforms:
      android:
        package: com.donaldark.dev_to_dev
        pluginClass: DevToDevPlugin
      ios:
        pluginClass: DevToDevPlugin
